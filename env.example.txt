# ============================================================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE
# ============================================================================
# 
# Copie estas variáveis para a plataforma de deploy (Railway, Render, etc.)
# NUNCA commite valores reais no Git

# Connection String do MySQL
# Formato: Server=HOST;Database=NOME_DB;User=USUARIO;Password=SENHA;Port=PORTA;
CONNECTION_STRING=Server=localhost;Database=PacientesDb;User=root;Password=;Port=3306;

# Porta da aplicação (opcional, padrão: 5000)
# Em plataformas cloud, geralmente é definida automaticamente
PORT=5000

# Ambiente (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production

